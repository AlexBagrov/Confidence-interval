# Подробное описание: Доверие к конверсии

Документ фиксирует, **зачем** сделан калькулятор, **что** он считает, **как** читать вывод и **где** его нельзя применять. Источник правды по коду — `index.html`.

## 1. Цель продукта

**Решение, которое число должно поддержать:** понять, какие долгосрочные уровни конверсии ещё совместимы с уже накопленными заявками — и отдельно: можно ли уже говорить о ширине оценки, о совместимости с ожидаемой C2 (средним направления) и о выполнении минимума плановой C2.

Это оценка неопределённости доли, не аттестация менеджера и не доказательство, что «конверсия равна X%».

Аудитория: руководитель или аналитик, который смотрит конверсию менеджера / направления на малой и средней выборке (десятки заявок), где «школьный» интервал `p ± z√(p(1−p)/n)` врёт.

## 2. Метрика и единица анализа

| Элемент | Как задано |
|---|---|
| Наблюдение | Одна заявка: успех или неуспех |
| Метрика | Доля продаж `k / n` (C2 — конверсия в продажу), не среднее процентов по людям и не среднее дневных конверсий |
| `n` | Целое число заявок ≥ 1 |
| `k` | Целое: `round(n × факт C2%)`. Если введённый процент на этом `n` невозможен, калькулятор округляет и пишет об этом |
| Ожидаемая C2 | Заданный типичный уровень направления / продукта. Считается известным, не оценивается из той же выборки |
| Плановая C2 | Порог «не ниже», не целевая точка «попасть в 12%» |

Вывод относится к той же популяции заявок, из которой взята выборка, и только при предпосылках ниже.

## 3. Предпосылки

Интервал имеет смысл, если:

1. Заявки однотипны (один продукт / направление, сопоставимый трафик).
2. Истинный уровень за период можно считать постоянным (нет явной смены процесса, сезона, отбора заявок).
3. Наблюдения независимы на уровне заявки (нет скрытых кластеров вроде «все заявки одного дня = одно событие»).
4. `n` зафиксирован заранее: это снимок, не правило «смотрим каждую неделю, пока не вылезет аномалия».
5. Среднее направления, если его используют как ориентир, само не оценено по крошечной выборке.

Если предпосылка не выполняется, формула Уилсона всё равно что-то нарисует — но это уже не ответ на исходный вопрос.

## 4. Метод

### 4.1. Основной: двусторонний интервал Уилсона для доли

Для заданного покрытия (90 / 95 / 99%) берётся `z` стандартного нормального распределения:

- 90% → 1.64485
- 95% → 1.95996
- 99% → 2.57583

Центр и полуширина:

```
denom  = 1 + z² / n
center = (p̂ + z² / (2n)) / denom
margin = z × √( (p̂(1−p̂) + z²/(4n)) / n ) / denom
CI     = [max(0, center − margin), min(1, center + margin)]
```

Почему не Wald (`p̂ ± z√(p̂(1−p̂)/n)`): на 10–50 заявках и у края (0% / 100%) Wald заужается, вылезает за [0, 1] и при нуле продаж схлопывается в точку. Уилсон остаётся внутри [0, 1] и при `k = 0` даёт ненулевую верхнюю границу.

Покрытие интервала — свойство процедуры на повторяющихся выборках, не «с вероятностью 95% истинная конверсия этого менеджера лежит внутри».

Подтверждение минимума плановой C2 по **нижней** границе двустороннего интервала намеренно строже одностороннего 5%-порога: калькулятор ставит вердикт «плановая C2 подтверждена», когда уровни ниже минимума уже плохо совместимы с фактом.

### 4.2. Вспомогательный: биномиальный хвост относительно среднего

Считает `P(K ≤ k)` или `P(K ≥ k)` при `K ~ Binomial(n, p_ожид)` через сумму в логарифмах (`log-sum-exp`), чтобы не потерять точность на малых вероятностях.

Это **пояснение**, насколько такой факт редок, если ожидаемая C2 верна. Вердикт «аномалия» ставится, только когда ожидаемая C2 **вне** интервала Уилсона. Хвост сам по себе не решение «внедрить / наказать / премировать».

При совпадении факта с ожидаемой C2 показывается вероятность ровно `k` успехов, не хвост.

### 4.3. Сколько ещё заявок

Двоичный поиск по `n` (верхняя граница поиска — 200 000):

| Функция | Вопрос |
|---|---|
| `nForPrecision(p, e, z)` | При каком `n` полуширина Уилсона в точке `p` станет ≤ порога `e` (по умолчанию ±5 п. п.) |
| `nToClearFloor(p̂, план, z)` | Если доля удержится около текущего факта, при каком `n` низ CI поднимется выше плана |
| `nZerosToRuleOut(p_ref, z)` | Сколько нулей продаж нужно, чтобы верх CI упал ниже ориентира (ожидаемой или плановой C2) |

Это расчёт под названную цель, не одно магическое «n, чтобы можно было доверять вообще».

Ориентир ±5 п. п. при покрытии 95% (те же числа, что в модалке справочника):

| Если уровень около | Нужно заявок |
|---|---|
| 5% | 83 |
| 10% | 141 |
| 15% | 196 |
| 20% | 245 |
| 30% | 320 |
| 50% | 381 |

Чем ближе доля к 50%, тем больше дисперсия и тем больше заявок на ту же полуширину.

## 5. Как устроен экран

Один файл `index.html`: разметка, стили и логика без сборки.

1. Форма ввода и кнопка «Рассчитать».
2. Крупный интервал и линейка 0–100% (полоса совместимых уровней, засечки факта C2 / ожидаемой C2 / плановой C2).
3. Карточки вывода (нейтральная / предупреждение / «ок») — формулировки, не светофор «человек хороший».
4. Таблица «Справочник точности»: сколько заявок нужно для ±N п. п. при ожидаемой C2, плановой C2 и факте C2.
5. Модалка «Когда данных хватает»: грубые корзины по `n` и таблица для ±5 п. п. / 95%.
6. Подвал с ограничениями (раскрывающийся список).

## 6. Правила копирайта (что пишет калькулятор)

Логика собрана так, чтобы число нельзя было принять за доказательство сверх модели.

### Точность оценки

- Внутри интервала, не у края: «интервал A–B, полуширина ±…». Если полуширина больше порога — оценка ещё широкая, цифру рано считать узкой. Если уже узкая — про ширину цифры факта; справляется ли менеджер и оставлять ли лидген — в блоках ожидаемой и плановой C2.
- `k = 0`: важна **верхняя** граница. «0% ± 5 п. п.» не читать.
- `k = n`: важна **нижняя** граница. «100% ± 5 п. п.» не читать.

### Ожидаемая C2 (среднее направления)

Двусторонняя совместимость с типичным уровнем.

- Ожидаемая C2 внутри CI → «ещё может объяснять факт» + хвост как частота.
- Ожидаемая C2 снаружи и факт выше → «аномалия вверх» (зелёный тон карточки).
- Ожидаемая C2 снаружи и факт ниже → «аномалия вниз» (предупреждение).
- При нуле продаж, если ожидаемая C2 ещё внутри: сколько ещё нулей нужно, чтобы верх CI отсёк ожидаемую C2.

«Аномалия» = типичный уровень с фактом плохо совместим. Это не доказательство, что менеджер «вырос» или «просел» как человек.

### Плановая C2 (минимум)

Порог «не ниже», не попадание факта в плановую точку.

- Вся полоса выше плановой C2 (`плановая < низ CI`) → минимум этими данными подтверждён.
- Вся полоса ниже плановой C2 (`плановая > верх CI`) → минимум не подтверждается.
- Плановая C2 внутри полосы → рано решать; если факт уже выше плановой C2, показывается, сколько заявок примерно нужно, чтобы низ CI тоже ушёл выше минимума.

Зелёный по плановой C2 — не «факт попал в 12%».

## 7. Чего метод не доказывает

- Что истинная конверсия «с вероятностью 95%» внутри интервала для этого человека.
- Что эффект есть, что человека нужно поощрять или наказывать.
- Что «не аномалия» = «всё в норме» и что «аномалия» = причина в менеджере (трафик, продукт, сезон, отбор заявок не закрыты).
- Каузальность: «после обучения конверсия выросла» из этого экрана не следует.
- Пригодность для A/B и раскатки: нет дизайна эксперимента, нет правила остановки.

Типичные ложные решения, если игнорировать ограничения:

- На 15 заявках с нулём продаж объявить менеджера безнадёжным, хотя верхняя граница ещё допускает двузначную конверсию.
- Увидеть ожидаемую C2 внутри полосы и решить, что «разницы нет».
- Каждую неделю пересчитывать тех же людей до первой красной карточки (подглядывание: заявленный уровень ложных «аномалий» уже не тот).
- Прогнать 20 менеджеров одним порогом и считать каждую аномалию сигналом (в среднем одна всплывёт и при норме).

## 8. Карта кода в `index.html`

| Блок | Роль |
|---|---|
| `wilson` | Интервал Уилсона, обрезка в [0, 1] |
| `nForPrecision` | Минимальное `n` для полуширины ≤ порога |
| `nToClearFloor` | Минимальное `n`, чтобы низ CI ≥ плана при том же `p̂` |
| `nZerosToRuleOut` / `zerosGap` | Сколько нулей нужно, чтобы верх CI отсёк ориентир |
| `binomTail` / `surpriseVsRate` | Поясняющий хвост бинома vs среднее |
| `pushExpected` / `pushPlan` | Формулировки карточек |
| `renderRefTable` | Справочник точности по ожидаемой C2 / плановой C2 / факту C2 |
| `fillGuidePrecision` | Таблица в модалке для ±5 п. п. и 95% |

Стили и тексты интерфейса живут в том же файле. Отдельного бэкенда, тестов и сборки нет.

## 9. Как развивать, не ломая смысл

Имеет смысл:

- Подставить другое покрытие или другой порог точности — это уже есть во вводе.
- Считать односторонний интервал **отдельно**, если цель только «подтвердить минимум», и явно подписать, что это другой вопрос.
- Добавить единицу анализа в подпись, если появятся сессии / дни вместо заявок.

Не имеет смысла без смены дизайна:

- Wald «для простоты».
- Светофор по p-value на дашборде команды.
- Байес со скрытым prior, выданный за «объективную вероятность».
- Сравнение двух менеджеров через «пересекаются ли их CI» вместо интервала на разность.

## 10. Связанные файлы

- `index.html` — рабочий калькулятор.
- `README.md` — короткий вход: запуск, поля, как читать, чего нет.
