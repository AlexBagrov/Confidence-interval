# Доверие к конверсии

Локальный калькулятор: по числу заявок и фактической C2 строит **интервал Уилсона** — какие долгосрочные уровни ещё совместимы с этими данными.

Не отвечает «менеджер хороший / плохой». Отвечает, что ещё согласуется с выборкой, если заявки однотипны и уровень за период не менялся.

## Запуск

Откройте `index.html` в браузере. Сборки, сервера и зависимостей нет: один файл, HTML + CSS + JS.

## Что вводить

| Поле | Обязательно | Смысл |
|---|---|---|
| Заявки | да | Число наблюдений `n` (заявки, не менеджеры) |
| Факт C2, % | да | Фактическая C2 в этой выборке. На `n` округляется до целого числа продаж `k`. C2 — конверсия в продажу |
| Ожидаемая C2, % | нет | Среднее по направлению: типичный уровень, не мишень «попасть ровно» |
| Плановая C2, % | нет | Минимум компании: доля **не ниже** этого порога |
| Покрытие | да | 90 / 95 / 99% — свойство метода, не «вероятность 95% для этого человека» |
| Точность ±, п. п. | да (по умолчанию 5) | Размах, при котором оценку уже считают узкой |

## Что смотреть в результате

1. **Полоса интервала** — совместимые уровни. Крайние 0% и 100% читайте по верхней / нижней границе, не как «± точность».
2. **Ожидаемая C2 внутри полосы** — типичный уровень ещё может объяснять факт. Снаружи — «аномалия» (плохо совместим). Как редко такой факт бывает при этом уровне — подсказка, не вердикт.
3. **Плановая C2** — порог «не ниже». Зелёный, когда вся полоса уже выше минимума; красный — когда даже верх ниже плановой C2; иначе рано решать.

Справочник «когда данных хватает» — про **ширину оценки**, не про вердикт.

## Чего калькулятор не делает

- Не каузальный вывод и не оценка качества человека.
- Не правило остановки: не пересчитывайте каждую неделю до первой «аномалии».
- Не массовый скрининг команды одним порогом (у 20 человек «аномалия» появится и при норме).
- Не вердикт по ожидаемой C2 с малой выборки: ожидаемую C2 (среднее направления) считайте заданной. Если она сама с малой выборки — не используйте «аномалию» как вердикт.

Подробности метода, копирайта и ограничений — в [ОПИСАНИЕ.md](ОПИСАНИЕ.md).
